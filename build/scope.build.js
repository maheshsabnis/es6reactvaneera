'use strict';

function printValue(x) {
    if (x) {
        var _i = 10;
        console.log('In-side if  ' + _i);
    }
    console.log('Out-side if  ' + i);
}

printValue(true);
printValue(false);
